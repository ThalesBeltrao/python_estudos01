# repo_estudo01

# 🐍 Python: Jornada do Intermediário à Orientação a Objetos

Este repositório é um registro detalhado da minha evolução técnica em Python, focado em conceitos avançados da linguagem e na aplicação prática de paradigmas de programação. Cada arquivo contém documentação in-code para explicar o comportamento interno do interpretador.

## 📂 Estrutura de Estudos

### 1. Nível Intermediário (Lógica e Funcional)
Focado em manipular dados de forma eficiente e entender o escopo de execução:
* **Funções & Closures:** Estudos sobre Higher Order Functions e como o Python gerencia memória para salvar valores em escopos fechados (Closures).
* **Iteráveis e Iteradores:** Implementação e uso de protocolos `__iter__` e `__next__` (arquivos: `count-py.py`, `iterator001.py`).
* **Tratamento de Dados:** Uso avançado de `map-filter-zip`, `dicionario`, `set`, e `list comprehension`.
* **Módulos e Pacotes:** Organização de código em `dados_packege` e uso de `modulos` built-in.
* **Controle de Exceções:** Robustez de código com `Try&Except`.

### 2. Orientação a Objetos (OOP)
Exploração profunda dos pilares da POO para criação de sistemas escaláveis:
* **Fundamentos de Classe:** Uso de `classes`, `metodo`, `construtores` e o atributo especial `__dict__`.
* **Relacionamentos:** Diferença prática entre `associacao`, `agregacao` e `composicao`.
* **Pilares POO:** Implementações de `heranca`, `polimorfismo`, `abstracao` e `encapsulamento`.
* **Recursos Avançados:** Uso de `decoradores`, `setter-getter`, `enum`, e organização com `docstrings`.
* **Persistência e Arquivos:** Manipulação de `arqjson` e `textmanager` dentro do contexto de objetos.

## 🛠️ Diferenciais Técnicos
* **Comentários Didáticos:** O código não apenas executa, mas explica o *porquê* de cada decisão técnica.
* **Foco em Back-end:** Estrutura pensada para quem busca especialização em arquitetura de sistemas e aplicações cloud-native.

---
**Perfil:** [Thales Willian](https://github.com/ThalesBeltrao) | Desenvolvedor Back-end